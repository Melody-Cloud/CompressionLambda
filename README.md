# Compression lambda

This is code for "compression lambda" used in "Melody Cloud" project. 

Its purpose is to run ffmpeg commands inside AWS Lambda runtime.

It can be deployed manually to AWS Lambda just by zipping the code (along with ffmpeg binary) and uploading it in AWS Lambda function console